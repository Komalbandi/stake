import { BackEndServiceGet } from './back-end-service-get';

describe('BackEndServiceGet', () => {
  it('should create an instance', () => {
    expect(new BackEndServiceGet()).toBeTruthy();
  });
});
